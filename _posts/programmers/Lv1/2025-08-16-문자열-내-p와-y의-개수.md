---
layout: post
related_posts:
  - /programmers/lv1
title:  "문자열 내 p와 y의 개수"
date:   2025-08-16
categories:
  - programmers
  - lv1
description: >
  java, javascript, python으로 풀이
---
* toc
{:toc .large-only}

## Java
```java
class Solution {
    boolean solution(String s) {
        boolean answer = true;
        char[] ar = s.toUpperCase().toCharArray();
        int numP = 0;
        int numY = 0;
        
        for(char c : ar) {
            if(c == 'P') {
                numP++;
            } else if(c == 'Y') {
                numY++;
            }
        }

        return answer = numP == numY;
    }
}
```
