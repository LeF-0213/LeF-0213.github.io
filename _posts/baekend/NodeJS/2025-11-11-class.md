---
layout: post
related_posts:
    - /backend/nodejs
title:  "[Node.js]클래스(Class)"
date:   2025-11-11
categories:
  - backend
  - nodejs
description: >
  
---
* toc
{:toc .large-only}

# 클래스
클래스(Class)는 객체지향 프로그래밍에서 객체를 만들기 위한 설계도 또는 틀입니다. 하나의 클래스는 공통된 속성과 동작을 가진 객체들을 정의하며, 이 클래스를 바탕으로 실제 사용할 수 있는 객체(인스턴스)를 생성한다. 예를 들어, 학생이라는 클래스를 만들면, 이름, 나이 같은 속성과 공부한다는 행동을 포함시킬 수 있고, 이 클래스를 사용해 여러 명의 학생 객체를 만들 수 있다. 클래스는 코드의 재사용성, 확장성, 유지보수성을 높여주며, 객체지향 프로그래밍의 핵심 구조로 동작한다.

```js
class 클래스이름 {
  constructor(매개변수들) {
    // 객체 초기화 (속성 정의)
  }

  메서드이름() {
    // 객체가 할 수 있는 동작 정의
  }
}
```
## 클래스 문법 요소
### constructor()
객체가 생성될 때 자동으로 호출되는 초기화 함수

### 인스턴스 메서드(클래스 내부 함수)
클래스 안에 constructor 외의 함수는 메서드로 등록된다.

### this 키워드
생성된 객체 자신을 가리킨다.

### 클래스 상속(extends)

### super() - 부모 클래스 constructor 호출

### 정적 메서드(static)

### 클래스 필드 (속성)정의 (ES2022 이후)

### get과 set

### private 필드
